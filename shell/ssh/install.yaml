- create:
    ~/.ssh:
      mode: 0700
- link:
    ~/.ssh/config:
      path: shell/ssh/config
    ~/.ssh/home.config:
      path: shell/ssh/home.config
    ~/.ssh/cloud.config:
      path: shell/ssh/cloud.config
